import 'package:wear_os_bible_ereader/app/app.dart';
import 'package:wear_os_bible_ereader/bootstrap.dart';

void main() {
  // Staging could provide a different repository implementation.
  // For example, it could use a different package for Data Layer communication
  // (like https://pub.dev/packages/watch_ble_connection) or provide a custom
  // backend that would allow iOS or web apps to send epub files to Wear OS app.
  // bootstrap(() => const App());
}
